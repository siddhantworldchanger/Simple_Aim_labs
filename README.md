Firstly install node.js (Ultra Nuub Step)
Secondly create a folder 
And bash command npm init
Then create a new sub folder name public and store all the files except server.js
and store server js in the npm main folder
bash these two commands npm install express and npm install three
then save all the files and run this bash command in the cmd node server.js
done 
